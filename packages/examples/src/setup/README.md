## scripts

```bash
{
   yarn rue:setup -f activerecord ./src/setup
   yarn rue:setup -e js -f activerecord ./src/setup
   yarn rue:setup -f activemodel ./src/setup
   yarn rue:setup -e js -f activemodel ./src/setup
   yarn rue:setup -f activeform ./src/setup
   yarn rue:setup -e js -f activeform ./src/setup
}
```
